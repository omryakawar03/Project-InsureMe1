apiVersion: v1
kind: Service
metadata:
  name: insureme-service
  namespace: insureme-ns
spec:
  type: LoadBalancer
  selector:
    app: insureme
  ports:
    - port: 8089
      targetPort: 8089
      protocol: TCP