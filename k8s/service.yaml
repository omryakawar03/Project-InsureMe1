apiVersion: v1
kind: Service
metadata:
  name: insureme-service
  namespace: insureme-ns
spec:
  type: LoadBalancer
  selector:
    app: insureme
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP